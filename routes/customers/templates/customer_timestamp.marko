<!doctype html>
html
    head
        title -- Customer Timestamp
    body
        p -- Customer ID: ${data.timestamp[0].customer_id}
        p -- Created At: ${data.timestamp[0].created_at}
        p -- Updated At: ${data.timestamp[0].updated_at}
